
<h1> The Tech Blog </h1>

{{#each posts}}
    <section class="card">
        <h2 class="card-title">{{title}}</h2>
        <p id="content">{{content}}</p>
        <p>User: {{user.username}}</p>
        <a href="/post/{{id}}">View Post</a>
    </section>
{{/each}}
